import { Todo } from '@/types'
import React from 'react'
type ButtonWrapperProps ={
    id:string 
    activeTodo:Todo
}

export default function ButtonWrapper({id,activeTodo}:ButtonWrapperProps) {
  return (
    <div></div>
  )
}
